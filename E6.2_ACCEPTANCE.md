# E6.2 — Task Discovery Granularity & Chapter/Task Separation

**目标：Chapter ≠ Task；Chapter status = aggregate(Task statuses)。**
我们保持 video runtime（`e2/e2_headed_gha.py`）完全不变，只在
「Task 模型 / reconcile / 队列」层做分离与聚合。

---

## 1. 为什么旧 discovery 只生成 1 个 chapter TaskInfo（§1）

根因（代码确认）：
`tvdp/tdvp.py::build_tasks_from_discovery(old)` 把**每一个目录节点
（`.posCatalog_select`）当作一个 task 直接映射为 `TaskInfo(task_type="video")`，
每章恒 1 个 task**。它没有把 node 内可能存在的多个实际 job/task 点拆开。

证据：真实 live DOM 中每章其实带 `jobUnfinishCount`（hidden input，= 待完成任务点数）
和 `catalog_points` 标记。例：
- `1217304705`（计算机网络的体系结构）：`jobUnfinishCount=1` → 「1个待完成任务点」，但该章 video 已 SERVER_VERIFIED 完成。
- `1217304706`（物理层的主要任务）：`2个待完成任务点`。
- 已完成章（如 4700/4701）有 `icon_Completed`，无待完成计数。

旧实现把这些「待完成任务点」全吞进同一个 `video` task → 混淆了
**单个 video 已完成** 与 **整个 chapter 未完成**。

## 2-4. 两层模型 + 每 task 独立 status（§2/§3/§4）
- 数据层已有 `ChapterInfo/TaskInfo` 与 `TaskRecord.task_type` 字段，本次把它们真正用起来。
- `build_tasks_from_discovery`：每章默认产出 1 个 **video** task；
  若该章 `status!=completed` 或 `job_remaining>0`，再产出 1 个 **`{cid}:other`** task（`task_type="other"`，`status=PENDING`），
  明确表达「本章还差 N 个 task，其中 video 可能已完成」。
- `e6/reconcile.py`：`_make_discovered/_make_ui_completed` 保留 `task_type`；
  对非 video 新 task 直接置 `PENDING`（不自动执行）。

## 3. Chapter = aggregate(Task)（§7/§9）
- 新增 `e6/task_registry.chapter_aggregate_status()`：
  所有 task 都为 COMPLETED 章节才算 COMPLETED；有 RUNNING→RUNNING；有 FAILED→FAILED；否则 PENDING。
- `done_chapter_ids_from_registry()` 改为**按章聚合**：只有当该章「所有 task 都 COMPLETED+有证据」才计入 done。
  video 单独 COMPLETED 无法再把一个还有 `other` PENDING 的章判为完成（§9）。

## 4. Queue 只接受真正可执行 task（§8）
- `reconcile_queue`：只把 `task_type == "video"` 放进 READY；`other/unsupported` 不排队。
- `is_executable` 增加 `PENDING`（E6.1 §6「PENDING → 可进入 READY」）。

## 5. Runtime 不变（§5）
- `e2/e2_headed_gha.py` 与被支持的 video 播放/verification 逻辑未做任何修改。

---

## 真实案例回归（§10）：1217304705

场景：video 已完成（SERVER_VERIFIED），但章节仍有 1 个非视频待完成点。

`tests/test_task_granularity.py`：
- `test_discovery_emits_video_plus_residual_task`：discovery 产出 video + other 两个 task。
- `test_chapter_aggregate_is_completed_only_when_all_tasks_completed`：聚合=PENDING，done 不含 4705。
- `test_reconcile_queue_excludes_non_video_task`：other 不进队列。
- `test_registry_queue_skips_completed_video_but_chapter_not_done`：4705 不重选。
- `test_reconcile_real_case_4705`：端到端 reconcile → video COMPLETED 保留、other PENDING、
  章节聚合 PENDING、done 排除。

## 验证结果
```bash
python -m pytest tests/ -q
# 104 passed, 1 skipped（新增 7 项 E6.2 用例，无回归）
```