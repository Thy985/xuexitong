# E6.1 — Task State Reconciliation & Evidence-backed Queue Integrity

本阶段严格限定于「Task State Integrity + Failure Persistence + Queue
Reconciliation + Evidence-backed Completion」，未扩展任何浏览器/课程执行能力。

## 一句话目标
建立可靠的 Task State Machine + Reconciliation + Evidence 完成语义，
消除「状态污染」与「失败任务永久卡在队列头部」。

## 验收对照（§15, 13 项）

| # | 验收项 | 实现/证据 | 状态 |
|---|--------|-----------|------|
| 1 | `nextUnit` 不再产生 completion | `e6/reconcile.py`；`run_test` 的 nextunit 只记录为诊断事件，绝不 `mark_completed`；测试 `test_nextunit_url_change_never_completes` | ✅ |
| 2 | `URL chapterId change` 不再产生 completion | 同上；唯一 `COMPLETED` 赋值在 `mark_completed()` 内 | ✅ |
| 3 | `COMPLETED` 必须具备 completion evidence | `mark_completed(*, evidence=...)` 无证据抛 `ValueError`；测试 `test_mark_completed_requires_evidence` | ✅ |
| 4 | FAIL 会真正写回 registry | `app/run.py` postflight 失败分支调 `mark_failed` 并 `save_registry`；测试 `test_cmd_run_fail_marks_registry`、`test_runtime_fail_writes_back` | ✅ |
| 5 | `consecutive_failures` 正确递增 | `TaskRecord.mark_failed` 每次 +1；测试 `test_mark_failed_increments_and_keeps_stage` | ✅ |
| 6 | 超过阈值进入 BLOCKED | `mark_failed` 到 max_attempts → `BLOCKED`；测试 `test_threshold_reaches_blocked` | ✅ |
| 7 | FAILED task 不会永久占用队列头部 | `reconcile_queue` 过滤 `BLOCKED`，FAILED 到阈值跳过；`mark_failed` 上限后 `BLOCKED` 被跳过；测试 `test_blocked_task_skipped`、`test_failed_task_can_retry` | ✅ |
| 8 | 已完成 task 不会重新入队 | `reconcile_queue` 跳过 `COMPLETED`；测试 `test_completed_task_not_queued` | ✅ |
| 9 | Scheduler 能从 registry 恢复状态 | `_run_tdvp_probe` 每次 wake 从 registry 重建；测试 `test_scheduler_restart_recovers_from_registry` | ✅ |
| 10 | 底层 runtime evidence 不被 scheduler 覆盖 | `cmd_scheduler` 把 `evidence`（failure_stage/checks/passed/run_id）并入输出；workflow 诊断步骤 surface 它们 | ✅ |
| 11 | 历史污染可以被 repair | `state/migrations/repair.py`（CLI + report）；测试 `test_bad_done_ids_repaired` | ✅ |
| 12 | 已知 1217304708 错误完成问题有 regression | `test_nextunit_url_change_never_completes`（1217304721→1217304708 不误 COMPLETED） | ✅ |
| 13 | E1/E2/E3/E5 regression 全部通过 | `pytest tests/` → **97 passed, 1 skipped** | ✅ |

## 测试矩阵（§14）

Unit：
- Path 4: completion requires evidence ✅
- Path: nextUnit != completion ✅
- Path: failure increments counter ✅
- failure persists ✅
- threshold → BLOCKED ✅
- completed task not queued ✅
- failed task can retry ✅
- blocked task skipped ✅
- same course reconciliation ✅

Integration（mock 去浏览器可测试路径）：
- PASS → registry update（SERVER_VERIFIED / UI）✅
- FAIL → registry update（mark_failed + persist）✅
- PASS → nextUnit navigation does not contaminate state ✅
- previous bad done_ids → migration repair ✅
- scheduler restart → correct state recovery ✅

## 迁移/修复证据
`state/migrations/20260906T000243Z/report.json`（dry-run，当前 registry 无残留污染，downgraded=0）。

## 运行
```bash
python -m pytest tests/ -q            # 97 passed, 1 skipped
python -m state.migrations.repair      # 查看/修复历史污染（默认 dry-run）
python -m state.migrations.repair --apply  # 实际写回（谨慎：会改 canonical registry）
```